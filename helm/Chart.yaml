apiVersion: v2
name: certmanager-letsencrypt
description: |
  DEPRECATED — moved to somaz94/helm-charts (charts.somaz.blog). This local chart
  is no longer maintained. See helm/README.md for migration details.
type: application
version: 0.1.0
appVersion: "1.0.0"
deprecated: true
